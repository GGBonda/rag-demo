# ============================================================
# RAG 知识库 - Python 3.12 依赖清单 (精简版)
# ============================================================
#
# 安装说明:
#   pip install -r requirements.txt
# ============================================================

# LlamaIndex 核心
llama-index-core>=0.14.0


# 底层文档解析（仅核心，不装 ML 推理等重型依赖）
# unstructured<0.19.0 避免新版依赖 llvmlite>=0.46.0/numba>=0.63.0 在 Intel Mac 上无预编译 wheel
unstructured[pdf]>=0.16.0,<0.19.0

# 文档解析推理（ONNX 模型、布局检测等）
unstructured_inference>=1.1.0

# PostgreSQL 驱动（预编译版本，无需 pg_config）
psycopg2-binary>=2.9.0

# LlamaIndex PGVector 向量存储
llama-index-vector-stores-postgres>=0.8.0

# 显式约束 llvmlite/numba 版本，确保 Intel Mac (x86_64) 有预编译 wheel
# llvmlite>=0.46.0 / numba>=0.63.0 已不再为 Intel macOS 提供 wheel
llvmlite<0.46.0
numba<0.63.0

# NumPy — 锁定 <2.0，因为 PyTorch 2.2.x / onnxruntime 等依赖是用 NumPy 1.x 编译的，
# 在 NumPy 2.x 下运行会触发 ABI 不兼容错误。
numpy>=1.24.0,<2.0.0

# 环境变量
python-dotenv>=1.0.0
